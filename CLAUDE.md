# プロジェクトルール

## サブエージェント連携ルール

このプロジェクトでは3つのサブエージェント（Planner → Generator → Evaluator）がパイプラインとして連携する。メインエージェント（あなた）はオーケストレーターとして全体を制御する。

### 全体フロー

```
ユーザーの短いプロンプト
  ↓
[Planner] 仕様書を生成
  ↓
メインエージェントが仕様書を docs/spec.md に保存
  ↓
[Generator] スプリント1の機能を実装 → 自己評価レポート出力
  ↓
[Evaluator] スプリント1を評価 → 合格/不合格を判定
  ↓
不合格 → Generator に修正指示を渡して再実装 → 再評価（最大3回）
合格   → 次のスプリントへ（Generator → Evaluator を繰り返す）
  ↓
全スプリント合格 → ユーザーに完了報告
```

### 1. オーケストレーター（メインエージェント）の責務

- ユーザーのプロンプトを受け取り、Planner を起動する
- Planner の出力（仕様書）を `docs/spec.md` に保存する
- 仕様書のスプリント計画に従い、スプリントごとに Generator → Evaluator の順で起動する
- Evaluator の判定結果に基づき、次のアクションを決定する：
  - **合格**: 次のスプリントへ進む（または全スプリント完了ならユーザーに報告）
  - **不合格**: Generator にフィードバックを渡して修正を指示する
- 各スプリントのリトライは **最大3回** まで。3回不合格の場合はユーザーに状況を報告し判断を仰ぐ
- 進捗は TodoWrite で管理する

### 2. エージェント間のデータ受け渡しルール

#### Planner → Generator
- Planner の出力は `docs/spec.md` としてファイルに保存する
- Generator 起動時のプロンプトには以下を含める：
  - 仕様書のファイルパス（`docs/spec.md`）
  - 今回のスプリント番号
  - 実装対象の機能名

#### Generator → Evaluator
- Generator の自己評価レポートをそのまま Evaluator に渡す
- Evaluator 起動時のプロンプトには以下を含める：
  - 仕様書のファイルパス（`docs/spec.md`）
  - スプリント番号
  - Generator の自己評価レポート全文
  - アプリケーションの起動方法（該当する場合）

#### Evaluator → Generator（不合格時のリトライ）
- Evaluator の評価レポート全文を Generator に渡す
- Generator 起動時のプロンプトには以下を含める：
  - 仕様書のファイルパス（`docs/spec.md`）
  - スプリント番号と「リトライN回目」の明記
  - Evaluator の評価レポート全文（修正指示を含む）
  - 「評価レポートの修正指示に従って修正してください」という指示

### 3. ファイル構成ルール

```
docs/
  spec.md          # Planner が生成した仕様書
src/               # Generator が実装するソースコード
```

- 仕様書は必ず `docs/spec.md` に保存する
- Generator はプロジェクトに適した構成でソースコードを配置する
- 各スプリント完了時にコミットを作成する（コミットメッセージに `Sprint X: [機能名]` を含める）

### 4. エージェント起動テンプレート

#### Planner の起動
```
Agent(subagent_type=planner):
  「以下のプロンプトから製品仕様書を作成してください: {ユーザーのプロンプト}」
```

#### Generator の起動（新規スプリント）
```
Agent(subagent_type=generator):
  「docs/spec.md の仕様書に基づき、スプリント{N}の機能「{機能名}」を実装してください。」
```

#### Generator の起動（リトライ）
```
Agent(subagent_type=generator):
  「docs/spec.md の仕様書に基づき、スプリント{N}（リトライ{M}回目）の修正を行ってください。
   以下はEvaluatorの評価レポートです。修正指示に従って修正してください:
   {評価レポート全文}」
```

#### Evaluator の起動
```
Agent(subagent_type=evaluator):
  「docs/spec.md の仕様書に基づき、スプリント{N}の機能「{機能名}」を評価してください。
   アプリの起動方法: {起動コマンド}
   以下はGeneratorの自己評価レポートです:
   {自己評価レポート全文}」
```

### 5. 禁止事項

- Planner が技術的な実装詳細（DB設計、API設計、ライブラリ選定など）を仕様書に含めてはならない
- Generator が1スプリントで複数機能を同時に実装してはならない
- Evaluator の評価を経ずに次のスプリントへ進んではならない
- 不合格のスプリントを合格扱いにしてスキップしてはならない
- リトライ上限（3回）を超えてユーザーへの報告なく再試行してはならない
